spring:
  application:
    name: order-service
  config:
    import: configserver:http://localhost:8985
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8985}
      fail-fast: true
